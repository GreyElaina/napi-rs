{
  "name": "bench",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "vitest bench ",
    "build": "cargo run -p cargo-napi -- napi build --no-js --release"
  },
  "devDependencies": {
    "tinybench": "^6.0.0",
    "vitest": "^4.0.8"
  }
}
