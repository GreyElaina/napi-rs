{
  "name": "memory-testing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cargo run -p cargo-napi -- napi build --release --no-js"
  },
  "dependencies": {
    "colorette": "^2.0.20",
    "dockerode": "^5.0.0",
    "pretty-bytes": "^7.1.0",
    "table": "^6.9.0"
  },
  "devDependencies": {
    "@types/dockerode": "^4.0.0"
  }
}
